# média de um aluno

print('=='*60)
n1 = float(input('qual a nota do aluno no primeiro exame? '))
n2 = float(input('qual a nota do aluno no segundo exame? '))
med = (n1+n2)/2
print('A média entre {} e {} é: {:.1f}'.format(n1, n2, med))
